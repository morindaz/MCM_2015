
scale = 1;

theta = GetTheta(scale);

hold on;
rs = plotBoundary(theta,scale); %ªÒ»°±ﬂΩÁ

index = 10;

plot(rs(1,index),rs(2,index),'r+');

x1 = rs(:,index-1);
x2 = rs(:,index+1);

k = (x2(2)-x1(2))/(x2(1)-x1(1));

